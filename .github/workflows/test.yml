name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run
        uses: ./

      - name: Complete
        run: echo "test is complete."